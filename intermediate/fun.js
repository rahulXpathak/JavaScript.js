function calculateSquare(num){
    //logic to calculate square of a number 
    let squareValue = num * num;

    //return the output of the squre value 
    return squareValue;
}
let result1 = calculateSquare(9);
console.log(result1);
let result2 = calculateSquare(5);
console.log(result2);

console.log(calculateSquare(6))


//return keywords not madnatory ;
function calculateSquare(num){
    //logic to calculate square of a number 
    let squareValue = num * num;

    // output of the squre value 
    console.log(squareValue);
}
calculateSquare(9) // actual input we are passing to the functions 
calculateSquare(7) // here 9 and 5 is arguemt 

//Arguments and parameter to a functions 
/* when we define a functions , we define the input values , that the function expects , 
these are not actual values insred just a placeholder that will later store actual values   */

